This includes:

- Preprocessing code from https://github.com/bshall/ZeroSpeech but with some errors fixed for the latest dependencies
  and input/output dir modified
- Script to encode zerospeech data using model trained using code from `vq_vae`
